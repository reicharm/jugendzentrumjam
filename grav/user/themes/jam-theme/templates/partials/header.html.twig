<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if header.title %}{{ header.title }} – {% endif %}{{ config.theme.site_title }}</title>
    {% if page.summary %}<meta name="description" content="{{ page.summary|striptags }}">{% endif %}
    <link rel="stylesheet" href="{{ url('theme://css/theme.css') }}">
</head>
<body>
<header class="site-header">
    <div class="wrap">
        <a class="logo" href="{{ base_url }}/">
            {# Platzhalter, bis das echte Logo geliefert wird #}
            <span class="logo-text">j@m</span>
            <span class="logo-sub">{{ config.theme.site_claim }}</span>
        </a>
        <nav class="main-nav">
            <a href="{{ base_url }}/">Start</a>
            <a href="{{ base_url }}/neuigkeiten">Neuigkeiten</a>
            <a href="{{ base_url }}/termine">Termine</a>
            <a href="{{ base_url }}/ueber-uns">Über uns</a>
        </nav>
    </div>
</header>
<main class="wrap">
